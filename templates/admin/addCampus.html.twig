{% extends 'base.html.twig' %}

{% block body %}

    <h2>Ajoutez un nouveau campus :</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Créer</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_main') }}">
        <button>Annuler</button>
    </a>
{% endblock %}

{% block title %}
    | add campus
{% endblock %}

